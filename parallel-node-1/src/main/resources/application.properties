# ZooKeeper配置（所有节点共享同一个）
zookeeper.address=127.0.0.1:2181
zookeeper.session.timeout=30000
zookeeper.connection.timeout=10000

# Redis配置（所有节点共享同一个）
redis.host=127.0.0.1
redis.port=6379
redis.database=0
redis.password=

# 节点配置
node.id=node-1
# 设置为不主动参与Master选举（让node3成为主节点）
master.auto.elect=false
